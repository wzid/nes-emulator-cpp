# nes-emulator-cpp
An NES Emulator written in C++

## Running Tests
I use the CMake VSCode extension to run tests. You can also run the tests from the command line with the following commands:
```bash
$ cmake -S . -B build
$ cmake --build build
$ cd build && ctest
```